<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:icon="@drawable/menu_dots">

    <item
        android:id="@+id/back_to_details"
        android:title="@string/back_to_details"
        app:showAsAction="never" />

    <item
        android:id="@+id/back_to_trip_list"
        android:title="@string/back_to_list"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_signout"
        android:title="@string/signout"
        app:showAsAction="never" />

</menu>